# Changelog

## Versioning Policy
- Do not collect changes under an `Unreleased` section.
- Put every change under an explicit version heading.
- Use `0.0.1` for the initial change set.
- After each commit, continue with `0.1.x` patch versions for later changes.

## [0.0.1]
- Description: Clarified AGENTS.md completion and changelog versioning rules.
- file touched: AGENTS.md, docs/CHANGELOG.md
- reason: Keep agent verification requirements clear and prevent unversioned changelog buildup.
- problems fixed: Read-only tasks are no longer treated like code changes, exact
  test commands are listed, placeholder guidance is removed, and changelog
  entries must use explicit version headings.
